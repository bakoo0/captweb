# Data Storage

This directory holds the aggregated CSV export generated by the application. The server automatically creates `all_user_data.csv` with the appropriate header the first time it runs and appends session rows for every completed test run.
